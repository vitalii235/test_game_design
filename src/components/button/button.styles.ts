import styled from "styled-components";

export const Button = styled.button`
  background-color: #D0F83E;
  padding: 9px 17px;
  cursor: pointer;
  border-radius: 4px;
  border: 1px solid rgba(0, 0, 0, 0.08);
  font-size: 16px;
  color: #060606;
  font-family: 'Orbitron', sans-serif;
  font-weight: 800;
`;
